export enum DocumentType {
  CPF = 'CPF',
  CNPJ = 'CNPJ',
}

export enum UserRole {
  MASTER = 'master',
  ADMIN = 'admin',
  MANAGER = 'manager',
  EXECUTOR = 'executor',
  CONSULTANT = 'consultant',
}

export enum UserStatus {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
  PENDING = 'PENDING',
}
