name: Deploy to AWS ECS

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag da imagem Docker'
        required: true
        default: 'latest'

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: 114700956661
  ECR_REPOSITORY: tooldo-api
  ECS_CLUSTER: tooldo-api
  ECS_SERVICE: tooldo-api

jobs:
  test:
    name: Test and Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm run typecheck

      - name: Run linter
        run: npm run lint:check

      - name: Check formatting
        run: npm run format:check

      - name: Run tests
        run: npm run test:cov
        continue-on-error: true

  build-and-push:
    name: Build and Push to ECR
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image tag
        id: image-tag
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT
          fi

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.image-tag.outputs.tag }}
        run: |
          docker build \
            --platform linux/amd64 \
            --provenance=false \
            --sbom=false \
            -t $ECR_REPOSITORY:$IMAGE_TAG \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:latest \
            .

          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "ecr_registry=$ECR_REGISTRY" >> $GITHUB_OUTPUT

    outputs:
      image: ${{ steps.build-image.outputs.image }}
      image_tag: ${{ steps.build-image.outputs.image_tag }}
      ecr_registry: ${{ steps.build-image.outputs.ecr_registry }}

  deploy:
    name: Deploy to ECS
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Preflight (ECS service status before deploy)
        run: |
          echo "== describe-services (before) =="
          aws ecs describe-services \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_SERVICE }} \
            --region ${{ env.AWS_REGION }} \
            --query 'services[0].[status,desiredCount,runningCount,pendingCount,taskDefinition]' \
            --output table

          echo ""
          echo "== events (últimos 10) =="
          aws ecs describe-services \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_SERVICE }} \
            --region ${{ env.AWS_REGION }} \
            --query 'services[0].events[0:10].[createdAt,message]' \
            --output table

          echo ""
          echo "== tasks RUNNING =="
          aws ecs list-tasks \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service-name ${{ env.ECS_SERVICE }} \
            --region ${{ env.AWS_REGION }} \
            --desired-status RUNNING \
            --output table || true

      - name: Update ECS service
        run: |
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.ECS_SERVICE }} \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

      - name: Wait for service to stabilize
        run: |
          echo "⏳ Aguardando estabilização do serviço..."
          set +e
          aws ecs wait services-stable \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_SERVICE }} \
            --region ${{ env.AWS_REGION }} \
            --delay 30 \
            --max-attempts 40
          WAIT_EXIT_CODE=$?
          set -e

          if [ "$WAIT_EXIT_CODE" -ne 0 ]; then
            echo "❌ Serviço não estabilizou (exit code: $WAIT_EXIT_CODE). Diagnóstico rápido:"
            echo ""

            echo "== describe-services (deployments) =="
            aws ecs describe-services \
              --cluster ${{ env.ECS_CLUSTER }} \
              --services ${{ env.ECS_SERVICE }} \
              --region ${{ env.AWS_REGION }} \
              --query 'services[0].deployments[*].[id,status,rolloutState,rolloutStateReason,taskDefinition,desiredCount,runningCount,pendingCount,failedTasks]' \
              --output table || true

            echo ""
            echo "== events (últimos 15) =="
            aws ecs describe-services \
              --cluster ${{ env.ECS_CLUSTER }} \
              --services ${{ env.ECS_SERVICE }} \
              --region ${{ env.AWS_REGION }} \
              --query 'services[0].events[0:15].[createdAt,message]' \
              --output table || true

            echo ""
            echo "== list-tasks (STOPPED, últimos 5) =="
            STOPPED_TASK_ARNS=$(aws ecs list-tasks \
              --cluster ${{ env.ECS_CLUSTER }} \
              --service-name ${{ env.ECS_SERVICE }} \
              --desired-status STOPPED \
              --region ${{ env.AWS_REGION }} \
              --query 'taskArns[0:5]' \
              --output text)
            echo "$STOPPED_TASK_ARNS"

            if [ -n "$STOPPED_TASK_ARNS" ] && [ "$STOPPED_TASK_ARNS" != "None" ]; then
              echo ""
              echo "== describe-tasks (STOPPED reason) =="
              aws ecs describe-tasks \
                --cluster ${{ env.ECS_CLUSTER }} \
                --tasks $STOPPED_TASK_ARNS \
                --region ${{ env.AWS_REGION }} \
                --query 'tasks[*].[taskArn,stoppedReason,containers[0].reason,containers[0].exitCode]' \
                --output table || true

              echo ""
              echo "== describe-tasks (STOPPED, raw) =="
              aws ecs describe-tasks \
                --cluster ${{ env.ECS_CLUSTER }} \
                --tasks $STOPPED_TASK_ARNS \
                --region ${{ env.AWS_REGION }} \
                --output json || true
            fi

            echo ""
            echo "Dica: a causa quase sempre aparece em STOPPED reason (ex.: health check falhou, falta de env/secrets, erro de conexão com DB)."

            echo ""
            echo "== CloudWatch logs (últimos 200 eventos, /ecs/tooldo-api) =="
            LOG_GROUP="/ecs/tooldo-api"
            # filter-log-events não depende de logStreamName (evita ResourceNotFoundException)
            aws logs filter-log-events \
              --log-group-name "$LOG_GROUP" \
              --limit 200 \
              --region ${{ env.AWS_REGION }} \
              --query 'events[*].message' \
              --output text || true
            exit $WAIT_EXIT_CODE
          fi

          echo "✅ Serviço estabilizado!"

      - name: Deployment summary
        run: |
          echo "## 🚀 Deploy Concluído!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Imagem:** \`${{ needs.build-and-push.outputs.image }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Cluster:** \`${{ env.ECS_CLUSTER }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Serviço:** \`${{ env.ECS_SERVICE }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
